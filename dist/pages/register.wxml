
    <view class="register-container">
        <view class="register-main">
            <view class="avatar-box">
                <image src="../images/logo2.jpg"/>
            </view>
            <view class="info">
                欢迎访问个性化学习中心
            </view>
            <view class="info">
                请先输入您的信息完成绑定
            </view>
            <view class="input-box">
                <input type="idcard" value="{{idCard}}" placeholder="请输入您的身份证号码" bindinput="idCardInput"/>
            </view>
            <view class="input-box">
                <input type="text" value="{{name}}" placeholder="请输入您的姓名" bindinput="userNameInput"/>
            </view>
            <view class="btn-box">
                
    <view class="rich-button {{$richButton$size}} {{$richButton$theme}}" bindtap="$richButton$onTap" style="{{$richButton$width ? ('width:' + $richButton$width) : ''}}">
        <image src="{{$richButton$iconSrc}}"  wx:if="{{$richButton$iconSrc}}"/>
        {{$richButton$text}}
    </view>

            </view>
        </view>
        <image class="page-bg" src="../images/bg.jpg"/>
    </view>